import { readFileSync, readdirSync } from 'node:fs'; import { fileURLToPath } from 'node:url'; import { describe, expect, it } from 'vitest';
const directory=fileURLToPath(new URL('../src/',import.meta.url));
describe('IMPM authority boundary',()=>it('imports no forbidden authority implementation and no MiningAdapterFramework',()=>{const source=readdirSync(directory).filter(n=>n.endsWith('.ts')).map(n=>readFileSync(`${directory}/${n}`,'utf8')).join('\n'); expect(source).not.toMatch(/from\s+['"][^'"]*(?:hardware_authority|resource_authority|power_authority|thermal_authority|health_authority|security_authority|certification_authority|arbitration_authority|benchmark_authority|workload_authority|scheduling_authority|observability_authority)[^'"]*['"]/); expect(source).not.toMatch(/new\s+(?:HardwareAuthority|ResourceAuthority|PowerAuthority|ThermalAuthority|HealthAuthority|SecurityAuthority|CertificationAuthority|ArbitrationAuthority|BenchmarkAuthority|WorkloadAuthority|SchedulingAuthority|ObservabilityAuthority|MiningAdapterFramework)\b/); }));
